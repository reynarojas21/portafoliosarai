//scroll top selection
const scrollUp = document.querySelector("#scroll-up")

//Function
scrollUp.addEventListener("click", () =>
     {window.scrollTo({top:0,
       left:0, behavoir:"smooth"});
      })